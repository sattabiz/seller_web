name: Gh-Pages

on:
  push:
    branches: [ gh-pages ]

jobs:
  generate_web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2 # Only works with v2
      - uses: subosito/flutter-action@v1
      - uses: ./.github/actions/custom-gh-pages